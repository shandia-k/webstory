import { TEXT_FIXED } from './textFixed';
import { TEXT_UI } from './textUI';
import { TEXT_CONTENT } from './textContent';

export const UI_TEXT = {
    FIXED: TEXT_FIXED,
    UI: TEXT_UI,
    CONTENT: TEXT_CONTENT
};
